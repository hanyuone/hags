import { Button } from "@/components/ui/button"

function App() {
  return (
    <>
      <div>
        <Button onClick={() => console.log('a sanity check')}>Click me</Button>
      </div>
    </>
  )
}

export default App
